<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.besby.poisoi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/znacok"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/znacok_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".groop1Activity" />
        <activity android:name=".groop2Activity" />
        <activity android:name=".dayActivity" />
        <activity android:name=".day2Activity"></activity>
    </application>

</manifest>